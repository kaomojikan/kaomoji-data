# Copy to `.env` and adjust to your local paths. `.env` is gitignored.

# Absolute path to the source site repo. Must contain `data/*.js` files
# that populate `window.KAOMOJI = { categories, items }`.
SITE=/absolute/path/to/your/source-repo
